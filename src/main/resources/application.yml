
spring:
  application:
    name: only-entry-service

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/entry_premiere
    username: root
    password: 1234

  data:
    redis:
      host: 127.0.0.1
      port: 6379
